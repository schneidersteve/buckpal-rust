[package]
name = "application"
version = "0.0.1"
edition = "2021"

[lib]
doctest = false

[dependencies]
domain = { version = "0.0.1", path = "../domain" }
chrono = "0.4"
async-trait = "0.1"

[dev-dependencies]
mockall = "0.11"
async-std = { version = "1", features = ["attributes"] }
